[Warnings]
# Omarchy's qs modules expose runtime-provided APIs without .qmltypes metadata.
# Keep syntax and import diagnostics enabled, but suppress false positives from
# dynamic plugin APIs and Loader.item.
MissingProperty=disable
UnqualifiedAccess=disable
UnusedImports=disable
